sudo apt-get install python3-pyqt5
sudo apt-get install python3-pyqt5.qtmultimedia python3-pip
sudo apt-get install ubuntu-restricted-extras
sudo apt-get install libgstreamer1.0-dev libgstreamer-plugins-bad1.0-dev libgstreamer-plugins-base1.0-dev libgstreamer-plugins-good1.0-dev ffmpeg
sudo apt-get remove gstreamer1.0-vaapi
sudo apt-install install sox

pip install -U git+https://github.com/Freemanium/govee_btled
pip install mediapipe matplotlib
pip install opencv-contrib-python

sudo sudo

python3 main.py& (we can add it to the Tahsin's shell script)

The segmentation fault error on exit is OK.
